<h1>All of your tasks!</h1>

<%= link_to 'New task', new_task_path %>
 
<table>
  <tr>
    <th>Title</th>
    <th>Deadline</th>
    <th></th>
  </tr>
 
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.title %></td>
      <td><%= task.deadline %></td>
      <td><%= link_to 'Show', task_path(task) %></td>
    </tr>
  <% end %>
</table>